import React from 'react';

const PlaygroundView = () => {
  return (
    <div data-testid="playground-view">
      <h2>Playground View</h2>
      <div data-testid="playground-editor-container"></div>
      {/* Add playground components here */}
    </div>
  );
};

export default PlaygroundView;
